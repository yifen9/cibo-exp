services:
  cibo-exp-dev:
    image: ghcr.io/yifen9/cibo-exp-dev
#     build:
#       context: ..
#       dockerfile: Dockerfile.dev
    container_name: cibo-exp-dev
    working_dir: /workspace
    volumes:
      - "..:/workspace"
    command: >
      bash -lc "
        just init &&
        exec sleep infinity
      "
